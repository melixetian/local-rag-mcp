Implement the mandatory scope in Spec.md in this repository. Read the specification and inspect the existing code before editing; treat Spec.md as the source of truth.

Keep the change minimal and compatible with the current CLI, RAG answer flow, and MCP behavior. Add the specified unit tests and update the documentation, but do not implement the optional benchmark or unrelated refactoring.

Do not run tests, the application, Ollama, the MCP server, index building, dependency installation, linters, or formatters. I will run everything locally and send you any output needed for debugging.

When finished, report:

1. files changed;
2. a concise summary of the implementation;
3. exact commands I should run to install dependencies, run tests, rebuild the index, and start the application;
4. any assumptions or remaining risks.
